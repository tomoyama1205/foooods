.shop-box.col-4.rounded
  =link_to shop_path(shop.id) do
    .shop-box__title
      = shop.title
    = image_tag shop.images[0].url, class: "shop-box__image"
    .shop-box__text
      = shop.text
    .star-box
      .star-rating
        .star-rating-item{style:"width:100%;"}